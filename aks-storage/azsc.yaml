apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: "managed-premium-retain-sc-cosmotal"
  labels:
     name: "app"
     env: "dev"
provisioner: disk.csi.azure.com
parameters:
 skuName: StandardSSD_LRS
reclaimPolicy: "Retain"
volumeBindingMode: "WaitForFirstConsumer"


 